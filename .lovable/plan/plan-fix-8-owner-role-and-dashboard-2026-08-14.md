# Plan: Fix 8 - Owner Role and Dashboard

## Summary
- Remove `admin` role from `krishnaaroraflp@gmail.com` and assign `tenant_owner`.
- Update `src/routes/_authenticated.tsx` to handle `tenant_owner` role and `/owner` route gating.
- Update `src/routes/admin.tsx` to strictly exclude non-admins.
- Create `/owner` route layout and dashboard (similar to `/admin` but scoped).
- Refactor `src/lib/access-codes.functions.ts` to support both `admin` (platform) and `tenant_owner` (scoped).
- Update RLS for `access_codes` to allow `tenant_owner` to manage their own codes.

## User Review Required
> [!IMPORTANT]
> - `krishnaaroraflp@gmail.com` will lose access to `/admin` and must use `/owner`.
> - Access codes generated by owners will be automatically tagged with their `coach_id`.

## Technical Details

### 1. Database Migration
- Update `user_roles` for `krishnaaroraflp@gmail.com`: delete `admin`/`platform_admin`, insert `tenant_owner`.
- Update `access_codes` RLS:
    - `SELECT`: `authenticated` can see their own codes (`coach_id = auth.uid()`) OR `has_role(auth.uid(), 'admin')`.
    - `INSERT`: `has_role(auth.uid(), 'admin')` OR `has_role(auth.uid(), 'tenant_owner')`.
    - `DELETE`: `(coach_id = auth.uid())` OR `has_role(auth.uid(), 'admin')`.

### 2. Role Gating & Navigation
- `src/routes/_authenticated.tsx`:
    - Add `tenant_owner` to allowed roles for `/owner`, `/coach`, `/dashboard`.
    - Redirect `tenant_owner` away from `/admin`.
- `src/routes/admin.tsx`:
    - Ensure `krishnaaroraflp@gmail.com` is removed from "fast-track" admin list.

### 3. Scoped Data Functions
- `getAccessCodes`: If `admin`, return all. If `tenant_owner`, filter by `coach_id`.
- `generateAccessCode`: Set `coach_id` to current user ID if they are a `tenant_owner`.

### 4. Owner Dashboard
- Create `src/routes/owner.tsx` (Layout) and `src/routes/owner/index.tsx` (Dashboard).
- Create `src/routes/owner/access-codes.tsx` (Scoped Access Codes).
- Link to these from the `/owner` sidebar.
